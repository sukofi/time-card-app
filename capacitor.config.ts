import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.attendanceapp',
  appName: 'AttendanceApp',
  webDir: 'dist'
};

export default config;
